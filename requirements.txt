# Core dependencies for the dataset pipeline
# (prepare / infer / filter / build / validate) and batch inference.
#
# Optional visualization/demo packages (aitviewer, moderngl-window, pyrender,
# gradio, HTML4Vision) are NOT listed here: moderngl-window 2.4.6 pins
# numpy<2, which is incompatible with the numpy 2.x this repo uses. Install
# them only if you need the viewers/demo, via requirements-viewer.txt with
# --no-deps. The pipeline does not import them.
numpy==2.2.6
opencv-python
decord
scikit-image
smplx==0.1.28
yacs
# mmcv==1.3.9
timm
einops
xtcocotools
pandas
hydra-core
hydra-submitit-launcher
hydra-colorlog
pyrootutils
rich
webdataset
ultralytics
pulp
supervision
pycocotools
joblib
natsort
# git+https://github.com/facebookresearch/pytorch3d.git@stable
# torch-scatter: if DPVO/DROID fails with libcudart.so.11.0, reinstall from PyG for your torch+CUDA — see README "Install DPVO".
torch-scatter==2.1.2
evo
pytorch-minimize
mmengine==0.10.4
plyfile
easydict
loguru
dill
lapx
# chumpy@git+https://github.com/mattloper/chumpy

## analyze:
zarr
seaborn
matplotlib
