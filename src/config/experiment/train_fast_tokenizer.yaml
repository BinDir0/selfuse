# @package _global_
name: fast_tokenizer
exp_name: "fast_tokenizer"
_target_: src.workspace.train_fast_tokenizer.TrainFastTokenizerWorkspace

image_shape: [224, 224, 3]
dataset_paths:
 - /share_data/datasets/taco/taco.zarr
 - /share_data/datasets/hoi4d/hoi4d_cleaned.zarr

shape_meta: &shape_meta
  obs:
    rgb:
      shape: ${image_shape}
      type: rgb
      horizon: ${n_obs_image_steps}
    state:
      wrist:
        shape: [18]
      hand: 
        shape: [30]
      shape: [48]
      type: low_dim
      horizon: ${n_obs_state_steps}
  action:
    shape: [48]
    horizon: ${n_action_steps}

dataset:
  _target_: src.dataset.legendvla_dataset.LegendVLAActionDataset
  zarr_paths: ${dataset_paths}
  horizon: ${eval:'${n_action_steps}+${dataset.history}'}
  pad_before: ${eval:'${dataset.history}+${n_latency_steps}'}
  pad_after: ${eval:'${n_action_steps}-1'}
  shape_meta: ${shape_meta}
  history: 30 # frames

n_action_steps: 30
n_obs_image_steps: 2
n_obs_state_steps: 6
n_latency_steps: 0
# Make sure history is divisible by (n_obs_image_steps - 1) and (n_obs_state_steps - 1)

dataloader:
  batch_size: 4096
  num_workers: 16
  shuffle: False
  pin_memory: True
  persistent_workers: True

tokenizer: 
  scale: 20
  vocab_size: 2048

training:
  tqdm_interval_sec: 1.0
  save_path: 

multi_run:
  run_dir: outputs/${now:%Y.%m.%d}/${now:%H.%M}_${name}_${hydra:runtime.choices.experiment}
  wandb_name_base: ${now:%Y.%m.%d-%H.%M}_${name}_${hydra:runtime.choices.experiment}

hydra:
  job:
    override_dirname: ${name}
  run:
    dir: outputs/${now:%Y.%m.%d}/${now:%H.%M}_${name}_${hydra:runtime.choices.experiment}
  sweep:
    dir: outputs/${now:%Y.%m.%d}/${now:%H.%M}_${name}_${hydra:runtime.choices.experiment}
    subdir: ${hydra.job.num}
