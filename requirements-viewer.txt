# Optional visualization / demo dependencies. NOT required to run the
# dataset pipeline (prepare/infer/filter/build/validate) or batch inference.
#
# moderngl-window 2.4.6 pins numpy<2, which is consistent with the numpy
# 1.26.4 pinned in requirements.txt, so these install cleanly. Use --no-deps
# if you want to avoid pulling any other transitive deps into the core env:
#
#   pip install --no-deps -r requirements-viewer.txt
#
# Their runtime deps (moderngl, pyglet, etc.) are already satisfied by the
# core environment or are listed explicitly below.
aitviewer
moderngl-window==2.4.6
moderngl
pyglet
pyrender
HTML4Vision
gradio
